{
  "name": "@marp-team/marp",
  "version": "0.0.0",
  "description": "The core of Marp: Marpit framework + Theme set",
  "main": "src/marp.js",
  "repository": "https://github.com/marp-team/marp/tree/master/packages/marp",
  "author": {
    "name": "Marp team",
    "url": "https://github.com/marp-team"
  },
  "contributors": [
    {
      "name": "Yuki Hattori",
      "url": "https://github.com/yhatt"
    }
  ],
  "license": "MIT",
  "scripts": {
    "build": "babel src --out-dir lib",
    "format": "prettier \"**/*.{md,json,css,scss}\"",
    "lint": "eslint --ignore-path ../../.eslintignore . && stylelint \"./**/*.{css,scss}\"",
    "test": "cross-env NODE_ENV=test mocha --opts ../../mocha.opts",
    "watch": "babel --watch src --out-dir lib"
  },
  "devDependencies": {
    "@researchgate/babel-plugin-transform-scss-import-to-string": "^0.1.2",
    "node-sass": "^4.8.3"
  },
  "dependencies": {
    "@marp-team/marpit": "^0.0.0"
  }
}
